import torch
import numpy as np
from tqdm import tqdm
import math
from torchvision.models.vgg import vgg16
import torch.utils.data as Data
import platform

a=torch.tensor([2,3])
if a is not None:
    print(2)